pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    parameters {
        choices(name: 'CHOICE', choices: ['build', 'test'], description: 'choice')
    }
    stages {
        stage('build') {
			when {
				environment name: 'CHOICE', value: 'build'
			}
            steps {
				echo "Executing build"
            }
        }

        stage('test') {
			when {
				environment name: 'CHOICE', value: 'test'
			}
            steps {
				echo "Executing test"                
            }
        }
    }
}